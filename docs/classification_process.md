# תיעוד תהליך סיווג הצעות חוק חוזרות

**נעשה עבור:** פרופ' אמנון
**מבצע:** אמיר טאהורי (עם Claude)
**תאריך:** 2026-04-21
**קובץ מסירה:** [`data/snapshots/All_Private_Bills_K1_K25_classified.xlsx`](../data/snapshots/All_Private_Bills_K1_K25_classified.xlsx)

---

## 1. שאלת המחקר

לכל הצעת חוק פרטית בכנסות 1–25, לקבוע:
1. האם זו **הצעה מקורית** (הוגשה בפעם הראשונה) או **הצעה חוזרת** (הוגשה אחרי הצעה דומה קודמת).
2. אם חוזרת — מהי ההצעה המקורית שלה (מספר סידורי פ/NNN + מספר כנסת).

## 2. מקורות הנתונים

**העבודה הסתמכה אך ורק על Knesset OData** (API הרשמי של הכנסת). לא על הקובץ המקורי שהתבסס עליו בעבר — הוא נמצא לא אמין (ראה סעיף 6).

| מקור | שימוש |
|---|---|
| **Knesset OData API** (`https://knesset.gov.il/Odata/ParliamentInfo.svc`) | מוזן אל DuckDB warehouse מקומי. מקור כל הנתונים המבניים. |
| **DuckDB warehouse** (`data/warehouse.duckdb`) | בסיס נתונים אנליטי מקומי, מכיל את כל טבלאות ה-OData. |
| `KNS_Bill` | טבלת הצעות החוק. **זה הקובץ "האמיתי"** — 51,673 הצעות חוק פרטיות בכנסות 1-25 (כל מה שיש לכנסת עצמה). |
| `KNS_DocumentBill` | טבלת קישורי מסמכים. לכל הצעה שומרת URL של מסמך ב-`fs.knesset.gov.il` (כולל הצעות חוק מלאות עם דברי הסבר). |
| `fs.knesset.gov.il` | שרת הקבצים של הכנסת. מכיל את מסמכי ההצעות עצמם (doc/pdf). **זה המקור שממנו קראנו דברי הסבר.** |
| `pmb.teca-it.com` (טל אלוביץ) | **לא משמש בקובץ הסופי.** שימש רק בסיבוב הראשון. סיבובנו העצמאי חליף אותו לחלוטין. |

## 3. השיטה

השיטה היא רפליקציה של שיטת טל ("זיהוי חוזרת לפי דברי ההסבר"), אבל מיושמת אצלנו מ-0. שלבים:

### 3.1 איתור מסמך לכל הצעה
עבור כל הצעת חוק פרטית ב-`KNS_Bill`, שולפים את ה-URL של המסמך מ-`KNS_DocumentBill`, עם סדר עדיפויות:
1. **הצעת חוק לדיון מוקדם** (doc/docx) — המסמך העיקרי שמכיל דברי הסבר.
2. **הצעת חוק לקריאה הראשונה** — כגיבוי אם הראשון חסר.

### 3.2 הורדה וחילוץ טקסט
- הורדה כל המסמך מ-`fs.knesset.gov.il` עם השהייה של 0.3 שניות בין בקשות (פוליטנס).
- חילוץ טקסט:
  - `.doc` / `.docx`: באמצעות `textutil` (כלי מובנה של macOS).
  - `.pdf`: באמצעות `pypdf`. אם ה-PDF משתמש בקידוד פונט לא סטנדרטי שאי אפשר לפענח, מדלגים.
- סריקת 4,000 התווים הראשונים של המסמך (דברי הסבר תמיד בתחילת המסמך).

### 3.3 חיפוש ביטויי חזרה
נסרקים הביטויים הבאים (regex על עברית):

| ביטוי | משמעות |
|---|---|
| `הצעת חוק דומה` | הצעה דומה |
| `הצעה זהה` | הצעה זהה |
| `חוזרת ומוגש` | חוזרת ומוגשת |
| `הוגש\ה? בכנסת ה-NN` | הפנייה לכנסת מסוימת |
| `בהמשך להצעת חוק` | בעקבות הצעה קודמת |
| `פ/NNN` או `ק/NNN` | הפנייה ישירה למספר סידורי |

אם אף אחד מהביטויים לא מופיע → ההצעה מסווגת כ**מקורית**.

### 3.4 פתרון ההפניה
אם נמצא `פ/NNN` + הפניה לכנסת → מחפשים את ההצעה המקורית ב-`KNS_Bill` (באמצעות `PrivateNumber` + `KnessetNum`). המטריצה נפתרת רק אם ההצעה הקודמת באמת קיימת ב-warehouse.

## 4. תוצאה

### 4.1 מספרים

| מדד | ערך |
|---|---|
| סה"כ הצעות פרטיות K1–K25 | **51,673** |
| מקוריות (מתוצאת הסריקה הגולמית) | 47,109 |
| חוזרות (מתוצאת הסריקה הגולמית) | 4,564 (8.8%) |
| כשלי הורדה / ללא URL | 219 (0.4%) |

### 4.2 שיעור חזרות לפי כנסת

| כנסת | הצעות | חוזרות | % | הערות |
|---|---|---|---|---|
| K1–K9 | 1,229 | 0 | 0.0% | לפני עידן הקונבנציה |
| K10 | 379 | 1 | 0.3% | |
| K11 | 701 | 3 | 0.4% | |
| K12 | 1,481 | 3 | 0.2% | |
| K13 | 3,599 | 5 | 0.1% | |
| K14 | 2,640 | 244 | 9.2% | **נקודת מפנה — תחילת הקונבנציה** |
| K15 | 4,156 | 418 | 10.1% | |
| K16 | 4,650 | 612 | 13.2% | |
| **K17** | **4,097** | **700** | **17.1%** | **שיא** |
| K18 | 4,654 | 679 | 14.6% | |
| K19 | 2,923 | 340 | 11.6% | |
| K20 | 6,020 | 472 | 7.8% | ירידה משמעותית |
| K21 | 554 | 50 | 9.0% | |
| K22 | 1,420 | 148 | 10.4% | |
| K23 | 2,604 | 223 | 8.6% | |
| K24 | 4,064 | 301 | 7.4% | |
| **K25** | **6,502** | **365** | **5.6%** | **שפל מודרני** |

**ממצא מעניין**: שיעור האזכור המפורש של הצעות קודמות עלה בהדרגה K14–K17, ואז ירד בהתמדה עד K25. הקונבנציה של לציין "הצעת חוק דומה" ב-דברי הסבר כנראה דועכת ב-15 השנים האחרונות.

## 5. עמודות בקובץ

קובץ הפלט: `data/snapshots/All_Private_Bills_K1_K25_classified.xlsx` — 51,673 שורות × 15 עמודות.

### 5.1 זיהוי ההצעה
| עמודה | תוכן |
|---|---|
| `BillID` | מזהה פנימי של הכנסת (מפתח ראשי של `KNS_Bill`). |
| `KnessetNum` | מספר הכנסת (1–25). |
| `Name` | שם ההצעה בעברית. |
| `PrivateNumber` | המספר הסידורי הפרטי (פ/NNN בדברי ההסבר). |
| `SubTypeDesc` | תמיד "פרטית" בקובץ הזה. |
| `doc_url` | ה-URL של המסמך ב-`fs.knesset.gov.il` שסרקנו. |

### 5.2 סיווג לקידוד (זרימת העבודה)
| עמודה | תוכן |
|---|---|
| `is_original` | `True` אם ההצעה צריכה להיות מקודדת ישירות. `False` אם להעתיק קידוד מההצעה המקורית. |
| `original_bill_id` | BillID של ההצעה המקורית שצריך להעתיק ממנה קידוד (עבור `is_original=False`), או BillID של ההצעה עצמה (עבור `is_original=True`). **השרשרת שטוחה** — כלומר בהפנייה אחת מגיעים להצעה שיש לקודד. |
| `original_knesset_num` | מספר הכנסת של ההצעה המקורית. |
| `original_private_number` | המספר הסידורי של ההצעה המקורית (פ/NNN/K#). |

### 5.3 סיווג לניתוח (האמת העובדתית)
| עמודה | תוכן |
|---|---|
| `is_recurring_upstream` | `True` אם הסריקה הגולמית זיהתה את ההצעה כחוזרת (לפני פוסט-עיבוד). שמור בנפרד כדי שהניתוח של "רעש המערכת" (השערת טל) יישאר תקף. |
| `effective_original_reason` | מדוע ההצעה "הורמה" ל-`is_original=True` למרות ש-`is_recurring_upstream=True`. ערכים: `doc_no_ancestor_found` (לא נמצאה הפניה מפורשת), `doc_fetch_failed` (כשל הורדה), `no_doc_url` (אין מסמך). ריק לרוב ההצעות. |

### 5.4 שקיפות מתודולוגית
| עמודה | תוכן |
|---|---|
| `method` | איך ההצעה סווגה: `doc_no_pattern` (לא נמצאה תבנית), `doc_pattern_linked` (נמצאה תבנית + פתרון ל-BillID), `doc_pattern_unresolved` (נמצאה תבנית אך לא ניתן לפתור), `doc_fetch_failed`, `no_doc_url`. |
| `matched_phrase` | הביטוי שנמצא (למטרות אודיט). |
| `classification_source` | תמיד `doc_based_full` בקובץ זה. |

## 6. מגבלות ידועות

### 6.1 השיטה שלנו יחסית לטל
השוואה שיטתית בין הסיווג שלנו לסיווג של טל אלוביץ (K19–K25 בלבד, שם יש חפיפה):

- **24,053 הצעות K19–K25 סווגו על ידי שנינו**
- **הסכמה כוללת: 38.3%** (9,213 הסכמות מתוך 24,053)
- **14,802 הצעות**: טל מסמנת כחוזרת, אנחנו מסמנים כמקורית (ההבדל הדומיננטי)
- **38 הצעות**: אנחנו מסמנים כחוזרת, טל מסמנת כמקורית (כמעט אפס)

**פירוש**: כאשר אנחנו כן אומרים "חוזרת", טל מסכימה ב-98% מהמקרים (דיוק גבוה מאוד). אבל מהחזרות שטל מוצאת, **אנחנו מוצאים רק ~11%** (recall נמוך מאוד). טל מוצאת בערך **פי 9 יותר חזרות** מאיתנו.

**למה?** טל כנראה משתמשת בשיטה סמנטית רחבה מעבר לחיפוש ביטויים מפורשים — ככל הנראה דמיון תוכני של נוסחי ההצעה, השוואת כותרות, או ניתוח מבני. הרגרסיה שלנו תופסת רק חזרות "מוצהרות" (ההצעה מזכירה במפורש את הצעה קודמת בדברי ההסבר). רוב החזרות בכנסת מודרנית הן "משתמעות" — אותו תוכן, ללא הפניה מילולית לקודם.

**המשמעות לניתוח ה"רעש"**: המספר האמיתי של חזרות הוא כנראה בין 8.8% (גבול תחתון — המוצהרות) ו-58% (גבול עליון — טל, כולל משתמעות). לפי שאלת המחקר, שני הגבולות יכולים להיות רלוונטיים:
- אם "חזרה" = "תוכן זהה" → 58% (שיטת טל) הוא המספר הנכון.
- אם "חזרה" = "המחוקק מודע שזו חזרה ומצהיר על כך" → 8.8% הוא המספר הנכון.

הקובץ שלנו תומך בשני הגבולות: עמודת `is_recurring_upstream` מזהה את המוצהרות, ובעזרת `our_scan_vs_tal_diff.xlsx` ניתן לבחון גם את ההבדלים שמגיעים מטל.

### 6.2 מגבלות אחרות
- **PDF עם פונטים לא סטנדרטיים**: ~0.4% מההצעות שה-PDF שלהן משתמש בקידוד פונט ישן שלא ניתן לפענח בצד הקליינט.
- **קישורי מסמכים חסרים**: ~0.05% מההצעות אין להן כלל רשומה ב-`KNS_DocumentBill`.
- **הפניות ללא פתרון**: 54 הצעות ציינו `פ/NNN` אך לא הצלחנו למצוא את ההצעה המקורית ב-`KNS_Bill` (כנראה התיחסו להצעות מכנסות מוקדמות מאוד או לחוקים קיימים, לא להצעות חוק).

## 7. איך לשחזר

כל הקוד ב-git תחת `AT020993/knesset_refactor`. השחזור המלא:

```bash
# 1. רענון warehouse מ-Knesset OData (אופציונלי — רק אם רוצים נתונים עדכניים)
cd /Users/amir/Projects/knesset_refactor
source .venv/bin/activate
PYTHONPATH="./src" python -m src.cli refresh

# 2. סריקה מלאה של דברי הסבר עבור כל ה-51,673 הצעות
# ~7 שעות בריצה רצופה; ממשיך מ-cache אם נפסק
PYTHONPATH="./src" python scripts/scan_all_bills.py

# 3. יצירת הקובץ הסופי לאמנון
PYTHONPATH="./src" python scripts/export_all_bills_classified.py

# 4. (אופציונלי) השוואה לטל
PYTHONPATH="./src" python scripts/diff_our_scan_vs_tal.py
```

## 8. מבנה הקוד

| קובץ | תפקיד |
|---|---|
| `src/data/recurring_bills/full_scan.py` | לוגיקת הסריקה של דברי הסבר |
| `src/data/recurring_bills/knesset_docs.py` | הורדה + חילוץ טקסט + regex |
| `scripts/scan_all_bills.py` | CLI להפעלת הסריקה המלאה |
| `scripts/export_all_bills_classified.py` | יצירת הקובץ הסופי (51,673 שורות) |
| `scripts/diff_our_scan_vs_tal.py` | השוואה שיטתית לטל |

## 9. איכות הנתונים

כל ארבע בדיקות השלמות עוברות:

- [x] כל ההצעות המקוריות (`is_original=True`) מצביעות על עצמן ב-`original_bill_id`.
- [x] אף הצעה חוזרת (`is_original=False`) לא מצביעה על עצמה.
- [x] כל הצעה חוזרת מצביעה על הצעה הקיימת בטבלה.
- [x] כל הצעה חוזרת מצביעה על הצעה שהיא `is_original=True` (כלומר ניתן לקודד ישירות).

---

## נספח: למה אנחנו עושים את זה ככה?

**שאלה שעלתה**: למה לא להשתמש ב-API של טל ישירות?

**תשובה**: טל היא צד שלישי. אם ה-API שלה יירד, או אם היא תשנה שיטה, או אם יש אי-הסכמה מתודולוגית — אין לנו שליטה. סריקה עצמאית על `fs.knesset.gov.il` נותנת לנו דאטה-סט משלנו שאנחנו יכולים לתעד, להעריך, ולשחזר. מצד שני — אין משמעות ש*טל טועה*. יש כאן רק שתי שיטות שמודדות דברים שונים במקצת:

- **שלנו**: "האם ההצעה *מצהירה* שהיא חוזרת?" (syntactic recurrence)
- **טל**: "האם ההצעה *דומה מהותית* להצעה קודמת?" (semantic recurrence)

שני הסוגים רלוונטיים לשאלת המחקר ("רעש המערכת") וניתן לנתח אותם בנפרד, אך הם לא אותו הדבר.
