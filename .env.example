# Environment configuration for local development
# Copy this file to .env and fill in your values
# The .env file is gitignored for security

# ==============================================================================
# Google Cloud Storage Configuration (for database sync)
# ==============================================================================

# Bucket name in Google Cloud Storage
GCS_BUCKET_NAME=your-bucket-name

# Path to service account credentials JSON file
# Option 1: Use GOOGLE_APPLICATION_CREDENTIALS (standard GCP convention)
GOOGLE_APPLICATION_CREDENTIALS=./path/to/service-account.json

# Option 2: Use GCS_CREDENTIALS_PATH (alternative name)
# GCS_CREDENTIALS_PATH=./path/to/service-account.json

# ==============================================================================
# How to get GCS credentials:
# ==============================================================================
#
# 1. Go to Google Cloud Console (https://console.cloud.google.com)
# 2. Select or create a project
# 3. Enable the Cloud Storage API
# 4. Create a bucket (or use existing one)
# 5. Go to IAM & Admin > Service Accounts
# 6. Create a service account with "Storage Admin" role
# 7. Create and download a JSON key file
# 8. Place the key file in this directory (it's gitignored)
# 9. Update the path above
#
# ==============================================================================
# Streamlit Cloud Configuration:
# ==============================================================================
#
# For Streamlit Cloud deployment, use .streamlit/secrets.toml instead:
#
# [storage]
# gcs_bucket_name = "your-bucket-name"
#
# [gcp_service_account]
# credentials_base64 = "base64-encoded-json-credentials"
#
# To generate base64:
#   cat service-account.json | base64 | tr -d '\n'
#
# ==============================================================================
