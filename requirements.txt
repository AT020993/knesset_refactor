# Core dependencies
streamlit>=1.44.0
duckdb>=1.2.0
pandas>=2.2.0
openpyxl>=3.1.0
pyarrow>=19.0.0
fastparquet>=2024.11.0

# Optional but recommended for better dev experience
watchdog>=4.0.0  # For faster hot-reloading in Streamlit

# Any additional dependencies (uncomment if used)
# aiohttp>=3.8.1   # If async fetch is used in the future
# requests>=2.27.1 # For HTTP requests if used
