{
  "name": "Knesset Refactor (Python 3.12)",
  // Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bookworm",
  "customizations": {
    "codespaces": {
      "openFiles": [
        "README.md",
        "src/ui/data_refresh.py"
      ]
    },
    "vscode": {
      "settings": {},
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "updateContentCommand": "python -m pip install --upgrade pip && [ -f requirements.txt ] && python -m pip install --user -r requirements.txt && [ -f requirements-dev.txt ] && python -m pip install --user -r requirements-dev.txt; echo '✅ Dependencies installed'",
  "postAttachCommand": {
    "server": "PYTHONPATH=./src streamlit run src/ui/data_refresh.py --server.enableCORS false --server.enableXsrfProtection false"
  },
  "portsAttributes": {
    "8501": {
      "label": "Application",
      "onAutoForward": "openPreview"
    }
  },
  "forwardPorts": [
    8501
  ]
}
